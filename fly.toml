app = "telegram-task-bot"

[build]
  dockerfile = "Dockerfile"

[env]
  DB_PATH = "/data/tasks.db"

[mounts]
  source = "data"
  destination = "/data"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
